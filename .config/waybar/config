{
	"position": "top",
	"margin-left": 10,
	"margin-right": 10,
  "margin-top": 10,
	"spacing": 10,
	"modules-left": ["wlr/workspaces"],
	"modules-center": ["wlr/taskbar"],
	"modules-right": ["tray", "cpu", "temperature", "memory", "network", "wireplumber", "clock"],
    "wlr/workspaces": {
         "disable-scroll": true,
         "all-outputs": true,
         "format": "{icon}",
         "on-click": "activate", // https://wiki.hyprland.org/Useful-Utilities/Status-Bars/#clicking-on-a-workspace-icon-does-not-work
         "persistent_workspaces": {
              "1": [],
              "2": [],
              "3": [],
              "4": [],
              "5": [],
          },
    },
  "wlr/taskbar": {
    "format": "{icon}",
    "icon-size": 15,
    "icon-theme": "Tela-nord-dark",
    "tooltip-format": "{title}",
    "on-click": "activate",
    "on-click-middle": "close",
    "on-click-right": "minimize",
    "ignore-list": [
       "foot"
    ],
  },
	"tray": {
    "icon-size": 15,
		"spacing": 5
	},
	"cpu": {
		"format": " {usage}%",
		"states": {
			"warning": 80,
			"critical": 90
		},
		"on-click-right": "footclient -a btm -T btm btm --default_widget_type cpu",
	},
	"temperature": {
		"hwmon-path": "/sys/class/hwmon/hwmon1/temp2_input",
		"critical-threshold": 80,
		"format-critical": "{icon} {temperatureC}°C",
		"format": "{icon} {temperatureC}°C",
		"format-icons": ["", "", "", "", ""],
		"on-click-right": "footclient -a btm -T btm btm --default_widget_type temperature",
	},
	"memory": {
		"format": " {percentage}%",
		"states": {
			"warning": 70,
			"critical": 90
		},
		"tooltip-format": "{used}GiB/{total}GiB",
		"on-click-right": "footclient -a btm -T btm btm --default_widget_type memory",
	},
	"network": {
		"interval": 5,
    "format": "{ifname}",
		"format-wifi": " {essid} {signalStrength}%",
		"format-ethernet": "",
		"format-linked": " No IP",
		"format-disconnected": "",
		"tooltip-format": "{ifname} {ipaddr}/{cidr} {gwaddr}",
		"format-alt": " {bandwidthUpOctets}  {bandwidthDownOctets}",
		"on-click-right": "footclient -a btm -T btm btm --default_widget_type network",
	},
  "wireplumber": {
    "format": "{volume}% {icon}",
    "format-muted": "",
    "format-icons": ["", "", ""],
    "on-click": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
    "on-click-middle": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
    "on-click-right": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"
  },
	"clock": {
		"format": " {:%H:%M}",
		"format-alt": " {:%Y-%m-%d}",
    "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
	},
}
